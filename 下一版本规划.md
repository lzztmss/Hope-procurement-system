# 心连心运营系统｜下一版本规划

> 本文记录已确认“本版暂不实现、下个版本再做”的事项，避免与当前测试版的功能收口混在一起。除特别说明外，以下内容不应改变现有业务数据与已上线流程。

## 一、串号（SN / IMEI）管理

### 目标

让可追溯设备做到“按台管理”，同时不让库存台账被大量单台设备记录淹没。

### 建议方案

- 库存台账继续以“产品 / 型号规格”为主，展示汇总数量，不逐台展开。
- 新增独立的“设备串号”查询入口：可按 SN、IMEI、产品、客户、出库单、售后单查询。
- 产品字典增加：`是否需要串号管理`、`串号类型（SN / IMEI / 其他）`。
- 采购到货或入库时，可按批次导入串号；系统校验导入数量与本次入库数量是否一致，并阻止重复串号。
- 出库时从本次可用设备中选择或扫描串号；售后时可通过串号直接查到该设备的入库、出库、客户与历史售后记录。

### 需确认

- 哪些产品必须管理串号：仅手表，还是床垫、套装等硬件也需要？
- 业务中以 IMEI、SN，还是两者都作为唯一识别号？
- 现有无串号库存是否需要补录，还是从新入库设备开始启用？

## 二、附件上传与归档

### 目标

将合同、报价、采购单、签收单、验收材料、售后图片等从“附件说明文字”升级为实际文件附件。

### 建议范围

- 在线索、销售订单、采购需求、出库交付、培训验收、售后工单中支持上传附件。
- 每个附件记录名称、上传人、上传时间、所属单据；可下载、预览和删除。
- 重要附件按业务权限控制查看，例如合同与报价只允许相关岗位查看。

### 需确认

- 哪些单据必须上传附件，哪些只是可选？
- 是否需要区分“内部附件”和“可给客户的附件”？

## 三、金额与敏感信息的后台权限

### 目标

正式版固定“无权限岗位不能获得金额字段”的规则，而不仅是页面上不显示。

### 建议规则

- 销售价格、订单金额：仅销售、业务统筹、公司领导、财务及管理员按确认范围查看。
- 采购单价、采购金额、付款信息：仅采购、财务、公司领导及管理员按确认范围查看。
- 其他岗位即使进入关联单据，也只看到与自己工作有关的产品、数量、状态和交付信息。

### 需确认

- 公司领导是否可查看所有销售与采购金额？
- 技术、仓库、售后在何种场景下需要看到金额；默认建议均不显示。

## 四、岗位权限模板与员工权限界面

### 目标

权限按岗位统一维护，管理员可在员工权限界面清楚地看到“可编辑 / 仅查看 / 不可见”，新增员工后直接继承岗位模板。

### 已确认原则

- 采购岗位不看线索台账和销售订单。
- 采购岗位可编辑其余业务模块：采购、库存、出库交付、培训验收、售后工单、产品字典、供应商交期、业务通报。
- 销售主要编辑线索、销售订单；对库存、出库、培训、售后按需要查看。
- 技术主要处理培训验收与售后；其他模块按需要查看。

### 下一步

- 将管理者确认后的岗位矩阵固化成模板，不再依赖逐人勾选大量权限。
- 员工权限页面改为按岗位展示模块：`不可见 / 查看 / 编辑`，管理员可在必要时调整岗位模板。

### 待管理者确认

- 采购岗位除采购、库存、产品字典、供应商交期外，是否需要编辑出库、培训、售后、业务通报？
- 仓库、技术、售后的查看范围与可操作节点分别是什么？
- 同一业务由多人协作时，采用“岗位均可处理”还是指定单据负责人后仅负责人可处理？

## 五、销售订单生成出库单的确认页

### 目标

销售从订单生成出库单时，不直接生成，而是打开一张已预填的“出库确认单”。

### 建议流程

1. 销售订单选择“生成出库单”。
2. 打开出库确认表，自动带出客户、地址、产品明细、数量、交付要求等订单数据。
3. 销售补齐发货方式、收货人、地址、培训需求等信息后提交。
4. 推送至出库交付；仓库在实际发货前再次确认出库，库存才扣减。

## 六、“系统录入”字段梳理

### 当前问题

“系统录入”容易被理解成所有出库都必须录入某个平台；选择“否”后，现有流程中也没有明确的业务差异。

### 下版建议

- 字段改名为“平台建档 / 系统开通”。
- 仅对确实需要开通平台或绑定第三方账号的产品显示。
- 选择“否”或“不适用”时，不创建任何平台建档待办；选择“是”时才推送给对应岗位处理。
- “第三方绑定”与“平台建档”分别说明，避免重复填写。

## 七、关联下拉框统一规则

### 目标

所有“关联线索、销售订单、采购需求、出库单”等下拉框只出现当前可继续处理的记录，避免完成、取消、关闭记录越积越多。

### 建议规则

- 默认仅显示进行中、待处理、待确认的关联单据。
- 已完成、已取消、已关闭单据不进入新建关联下拉框；历史记录仍可在查看页追溯。
- 若业务必须关联历史单据，使用“搜索历史单据”而不是默认下拉列表。

## 八、数据迁移与上线前准备

- 正式上线前整理产品、SKU、库存期初数、客户、未完成订单与未完成售后等基础数据。
- 串号启用后，先确定旧库存的补录边界，再导入新设备串号，避免账实混乱。
- 上线前用一份固定测试清单完整走通：线索、送样、销售、采购、入库、出库、培训、售后和权限。

## 本版暂不改动的原则

- 即时送样出库：线索填写“需要即时出库”后，生成待出库申请；仓库确认实际出库后才扣库存。
- 库存不足时不自动拆单或自动采购；由业务人员决定等待补货、修改数量或取消。
- 库存台账仍以产品 / 规格汇总为主，串号明细留待下一版本独立管理。
